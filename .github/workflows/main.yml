name: Build Project
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      with:
        submodules: true

    - name: Run build script
      run: |
        chmod +x ./build.sh
        ./build.sh
